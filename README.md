# Hackathon_Ing_SUAI - AI Document Processor

Современное веб-приложение для обработки документов с AI. **Два варианта фронтенда**: светлый и тёмный стиль.

## 📁 Структура Проекта

```
project-root/
├── client-light/          # Светлый дизайн с тёплыми цветами
│   └── src/
├── client-dark/           # Тёмный дизайн с синими и оранжевыми акцентами
│   └── src/
├── server/                # Express.js бэкенд
│   ├── routes.ts
│   ├── storage.ts
│   ├── swagger.ts
│   └── index-dev.ts
├── shared/                # Общие схемы и типы
│   └── schema.ts
├── vite.config.ts         # Default (light версия)
├── vite.config.light.ts   # Light версия (явный конфиг)
├── vite.config.dark.ts    # Dark версия
└── package.json
```

## 🎨 Два Варианта Дизайна

### Светлый (client-light)
- **Основной цвет**: Фиолетовый (#7c3aed)
- **Акцент**: Коралл (#ff6b6b)
- **Фон**: Тёплый крем (#f5f3f0)
- **Стиль**: Glassmorphism, градиентные сетки, мягкие тени

### Тёмный (client-dark)
- **Основной цвет**: Синий (#4099FF)
- **Акцент**: Оранжевый (#FFB84D)
- **Фон**: Глубокий чёрный (2px)
- **Стиль**: Glassmorphism, контрастные градиенты, глубокие тени

## 🚀 Установка и Запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск светлой версии (по умолчанию)
```bash
npm run dev
```

### 3. Запуск тёмной версии
```bash
npx vite --config vite.config.dark.ts --port 5000
```

Или добавьте скрипт в `package.json`:
```json
{
  "scripts": {
    "dev": "NODE_ENV=development tsx server/index-dev.ts -- --config vite.config.light.ts",
    "dev:dark": "NODE_ENV=development tsx server/index-dev.ts -- --config vite.config.dark.ts"
  }
}
```

### 4. Сборка для продакшена

**Светлая версия:**
```bash
npm run build
```

**Тёмная версия:**
```bash
vite build --config vite.config.dark.ts && esbuild server/index-prod.ts --platform=node --packages=external --bundle --format=esm --outfile=dist/index.js
```

### 5. Запуск продакшена
```bash
npm run start
```

## 🎯 Функции

✅ **Мульти-формат поддержка**: JPEG, PNG, GIF, WebP, PDF, ZIP
✅ **Drag & Drop загрузка**: Интуитивный интерфейс
✅ **Real-time обработка**: Статус обновляется в реальном времени
✅ **История задач**: Полная история всех обработок
✅ **REST API**: С документацией Swagger
✅ **Современный дизайн**: 2025 тренды в обоих вариантах
✅ **Glassmorphism**: Красивые стеклянные карточки
✅ **TypeScript**: Type-safe фронт и бэк

## 📄 API Endpoints

- `POST /api/upload` - Загрузка файлов
- `GET /api/status/:taskId` - Статус обработки
- `GET /api/tasks` - История задач
- `GET /api/download/:taskId` - Скачать результаты ZIP
- `GET /api-docs` - Swagger документация

## 🛠 Технологический Стек

### Frontend
- React 18 + TypeScript
- Vite + HMR
- Wouter (маршрутизация)
- TanStack Query (состояние)
- Shadcn UI + Tailwind CSS
- Lucide React (иконки)

### Backend
- Express.js
- Multer (загрузки)
- Archiver (ZIP)
- Swagger UI
- Drizzle ORM (готово для БД)

## 📖 Страницы

1. **Landing Page** - Красивый героёк с анимациями
2. **Upload & Process** - Загрузка и обработка файлов
3. **Task History** - История всех задач
4. **API Docs** - Документация и примеры
5. **About** - FAQ и информация о проекте

## 🔧 Конфигурация

### Переменные окружения

```bash
DATABASE_URL=postgresql://...   # Для БД (опционально)
SESSION_SECRET=your-secret      # Для сессий
NODE_ENV=development            # development или production
```

## 📝 Тесты

Добавьте `data-testid` для всех интерактивных элементов:

```bash
button-upload, input-email, link-profile
card-product-${id}, row-user-${index}
text-status, img-avatar
```

## 🚢 Деплой

1. Выберите нужный вариант (light или dark)
2. Сделайте сборку
3. Загрузите на хостинг
4. Запустите сервер

## 📚 Документация

Полная документация API доступна по адресу `/api-docs`

## 🎓 Hackathon

Это проектный работа для Hackathon_Ing_SUAI. 
Полностью функциональное MVP с современным дизайном и красивым UI.

## 📝 Лицензия

MIT

---

**Автор**: Hackathon_Ing_SUAI Team
**Дата**: Ноябрь 2025
**Статус**: Готово к демонстрации ✅
