# Hackathon_Ing_SUAI - AI Document Processor

## 📌 Обновление: Два варианта фронтенда!

Проект содержит **две независимые версии фронтенда**:
- **client-light** - светлый дизайн с фиолетовым (первоначальный)
- **client-dark** - тёмный дизайн с синим и оранжевым

Оба используют один и тот же бэкенд!

## 📁 Структура Проекта

```
project-root/
├── client-light/              # Светлый дизайн фронтенд
│   ├── src/
│   │   ├── pages/             # Все 5 страниц приложения
│   │   ├── components/        # Переиспользуемые компоненты
│   │   ├── lib/               # Утилиты (queryClient, hooks)
│   │   ├── App.tsx            # Главное приложение
│   │   └── index.css          # Глобальные стили (светлые)
│   └── package.json (часть общей)
│
├── client-dark/               # Тёмный дизайн фронтенд
│   ├── src/
│   │   ├── pages/             # Те же 5 страниц
│   │   ├── components/        # Те же компоненты
│   │   ├── lib/               # Те же утилиты
│   │   ├── App.tsx            # Главное приложение
│   │   └── index.css          # Глобальные стили (тёмные)
│   └── package.json (часть общей)
│
├── server/                    # Единый бэкенд Express.js
│   ├── routes.ts              # REST API endpoints
│   ├── storage.ts             # In-memory storage
│   ├── swagger.ts             # API документация
│   ├── index-dev.ts           # Dev entry point
│   └── index-prod.ts          # Prod entry point
│
├── shared/
│   └── schema.ts              # Zod схемы и типы
│
├── vite.config.ts             # Default (использует client-light)
├── vite.config.light.ts       # Light вариант
├── vite.config.dark.ts        # Dark вариант
├── tailwind.config.ts
├── tsconfig.json
├── package.json               # Общие зависимости
├── README.md                  # Документация для GitHub
├── replit.md                  # Этот файл
└── .gitignore
```

## 🎨 Два Варианта Дизайна

### Light (client-light)
- **Primary**: Фиолетовый (#7c3aed) → `260 100% 60%`
- **Accent**: Оранжевый (#FFB84D) → `30 100% 55%`
- **Secondary**: Оранжевый (#ff8c42) → `30 100% 55%`
- **Background**: Тёплый крем (#f5f3f0)
- **Foreground**: Тёмный текст
- **Стиль**: Glassmorphism, тёплые градиенты, мягкие тени

### Dark (client-dark)
- **Primary**: Синий (#4099FF) → `260 100% 60%`
- **Accent**: Оранжевый (#FFB84D) → `30 100% 55%`
- **Secondary**: Оранжевый (#FFB84D) → `30 100% 55%`
- **Background**: Глубокий чёрный (2px)
- **Foreground**: Светлый текст
- **Стиль**: Glassmorphism, контрастные градиенты, глубокие тени

## 🚀 Запуск Версий

### Светлая версия (по умолчанию)
```bash
npm run dev
```
Использует `vite.config.ts` который указывает на `client-light`

### Тёмная версия
```bash
npx vite --config vite.config.dark.ts
```

### Build светлой версии
```bash
npm run build
```

### Build тёмной версии
```bash
vite build --config vite.config.dark.ts && esbuild server/index-prod.ts --platform=node --packages=external --bundle --format=esm --outfile=dist/index.js
```

## 📄 Общая Информация

### Technology Stack

**Frontend (обе версии)**
- React 18 + TypeScript
- Vite с HMR
- Wouter маршрутизация
- TanStack Query (React Query)
- Shadcn/ui компоненты
- Tailwind CSS
- Lucide React иконки
- React Hook Form + Zod

**Backend (единый)**
- Express.js + TypeScript
- Multer для загрузок (50MB)
- Archiver для ZIP
- Swagger UI Express
- Drizzle ORM (готово для БД)
- In-memory storage (MemStorage)

### API Endpoints

- `POST /api/upload` - Загрузить файлы (images, PDF, ZIP)
- `GET /api/status/:taskId` - Получить статус обработки
- `GET /api/tasks` - Получить историю всех задач
- `GET /api/download/:taskId` - Скачать результаты ZIP
- `GET /api-docs` - Swagger UI документация

### Поддерживаемые форматы

- **Images**: JPG, PNG, GIF, WebP
- **Documents**: PDF
- **Archives**: ZIP (batch processing)
- **Max size**: 50MB на файл

## 🎯 5 Основных Страниц

**Обе версии имеют:**

1. **Landing Page** (`/`)
   - Героический раздел с анимациями
   - 6 карточек с features
   - Статистика и metrics
   - Bold CTA секция
   - Профессиональный footer

2. **Upload & Process** (`/upload`)
   - Drag-and-drop зона
   - Предпросмотр файлов
   - Real-time прогресс
   - Download после завершения
   - Reset функция

3. **Task History** (`/history`)
   - Полный список задач
   - Status badges
   - Фильтрация и сортировка
   - Скачать из истории
   - Empty state с CTA

4. **API Documentation** (`/api`)
   - Встроенный Swagger UI
   - Tabbed документация
   - Copy-to-clipboard примеры
   - Curl, JavaScript, Python примеры
   - Full OpenAPI 3.0 spec

5. **About** (`/about`)
   - How-it-works (3-шаг процесс)
   - Поддерживаемые форматы
   - FAQ accordion (5+ вопросов)
   - Tech stack информация
   - Hackathon информация

## 🔧 Разработка

### Hot Module Reloading
- Оба фронта поддерживают HMR
- Изменения отражаются мгновенно
- Состояние сохраняется

### Styling
- CSS переменные для тем
- Dark/Light HSL значения
- Tailwind utilities
- Custom animations

### Type Safety
- TypeScript везде
- Zod validation
- Shared schema.ts
- Strong typing на API

## 📋 Environment Variables

```bash
# Опционально (используется in-memory по умолчанию)
DATABASE_URL=postgresql://...

# Для сессий (если добавить auth)
SESSION_SECRET=your-secret-here

# Окружение
NODE_ENV=development  # или production
```

## 🎓 Для Hackathon

**Полностью готовое к демонстрации:**
- ✅ Два варианта дизайна на выбор
- ✅ Все функции работают
- ✅ Modern 2025 дизайн тренды
- ✅ Glassmorphism и красивые анимации
- ✅ Responsive на всех устройствах
- ✅ Dark mode встроен (в dark версии)
- ✅ API документация (Swagger)
- ✅ Real-time обработка файлов
- ✅ TypeScript type-safety
- ✅ Production-ready код

## 🌐 Готово к GitHub

**Структура оптимальна для:**
- Демонстрации (выбрать light или dark)
- Deployment на Vercel/Netlify
- Fork и модификации
- Портфолио проектов
- Production использования

## 📝 Ключевые Файлы

| Файл | Назначение |
|------|-----------|
| `vite.config.ts` | Default config (light) |
| `vite.config.light.ts` | Light версия |
| `vite.config.dark.ts` | Dark версия |
| `client-light/src/index.css` | Light стили |
| `client-dark/src/index.css` | Dark стили |
| `server/routes.ts` | API endpoints |
| `shared/schema.ts` | Data models |
| `README.md` | GitHub документация |

## 🚀 Next Steps

1. **Выбрать версию** (light или dark)
2. **Запустить приложение**: `npm run dev`
3. **Тестировать функциональность**
4. **Deploy на Replit/Vercel/Netlify**
5. **Share GitHub репозиторий**

**Всё готово!** Приложение полностью функционально и красиво оформлено. ✨
